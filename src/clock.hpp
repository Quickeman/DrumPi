// File: clock.hpp

#pragma once

#ifndef clock_h
#define clock_h

/*! Converts BPM to miliseconds. */
int bpmToMs(int bpm);

/*! Trigger repeated actions. */
class Clock {
// TODO
};

/*! Trigger a single delayed action. */
class Timer {
// TODO
};

#endif